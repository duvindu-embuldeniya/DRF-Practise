// //filter - values list
// //map - t/f list

// //find - 1st object which matches(value)

// //forEach - to iterate an array

// //some - if one of an object matches the condition(t/f)

// //every - if all the objects matches with the condition(t/f)

// //reduce - to take the sum of an array

// //sort -  a-b = (-) -> a,b   order|| 
//         //a-b = (0) -> a,b       ||
//         //a-b = (+) -> b.a       ||

// //include - for array and string 




